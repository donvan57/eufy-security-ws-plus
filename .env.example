# GitHub publishing credentials for this add-on repository.
# Keep the real .env local only; it is ignored by git.

GITHUB_USERNAME=your-github-name
GITHUB_REPO=eufy-security-ws-plus
GITHUB_TOKEN=github_pat_xxx

# Optional SSH-based publishing instead of token auth.
# GITHUB_SSH_KEY_PATH=/path/to/id_ed25519
